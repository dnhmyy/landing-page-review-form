# ─── Local Development ───────────────────────────────────────────────────────
# Untuk `npm run dev` dan `npx prisma migrate dev` secara lokal.
# Di Docker, DATABASE_URL dikompilasi otomatis dari DB_USER/DB_PASSWORD di docker-compose.yml

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/rotikebanggaan?schema=public"

NODE_ENV=development
ADMIN_SECRET=rk-admin-dev-secret
NEXT_PUBLIC_BASE_URL=http://localhost:3000