# ─── Roti Kebanggaan — Production Environment ─────────────────────────────
# Copy this file to .env and fill in real values before running docker compose up

# Database
DB_USER=rotikebanggaan
DB_PASSWORD=Tangerang0102!
DB_NAME=rotikebanggaan

# App (set to your VPS IP or domain — HTTPS handled externally by VPS firewall/proxy)
NEXT_PUBLIC_BASE_URL=http://your-vps-ip:3000

# Admin dashboard secret
ADMIN_SECRET=change_this_to_a_long_random_string

# Prisma (composed automatically from DB_* above in docker-compose)
# Only needed for running prisma commands locally outside Docker:
# DATABASE_URL=postgresql://your_db_user:your_strong_password@localhost:5432/rotikebanggaan?schema=public
